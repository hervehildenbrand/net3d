node_modules/
dist/
.env
*.log
.DS_Store
coverage/

# local env variants
.env.local
.env.*.local

# editors / os
.vscode/
.idea/
*.swp

# misc
# incl. packages/server/.cache/net3d — persisted SoT cache (per-instance)
.cache/
# user-edited floor plans (packages/server/.data/net3d-layouts) — local user data
.data/
tmp/
*.tgz

# internal planning notes — kept local, not published
specs/

# local-only operator tooling & dev ergonomics (kept out of the public repo)
/CLAUDE.md
/scripts/dev-restart.sh
/deploy/
